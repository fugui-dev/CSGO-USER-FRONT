# 加密功能排查指南

## 🔍 快速排查步骤

### 1. 检查环境变量配置

在项目根目录创建或检查 `.env` 或 `.env.development` 文件：

```env
# 启用加密功能
VITE_ENCRYPT_ENABLED=true

# 需要加密的URL路径（支持通配符）
VITE_ENCRYPT_URLS=/api/**
```

**重要提示：**
- 环境变量必须以 `VITE_` 开头才能在Vite项目中使用
- 修改环境变量后需要**重启开发服务器**
- 检查环境变量是否生效：在浏览器控制台输入 `import.meta.env.VITE_ENCRYPT_ENABLED`

### 2. 检查浏览器控制台日志

打开浏览器开发者工具（F12），查看控制台输出。你应该能看到类似这样的日志：

```
[加密调试] 加密功能已启用
[加密调试] 加密URL配置: /api/**
[加密调试] 请求路径: /api/user/login
[加密调试] 请求方法: post
[加密调试] 模式匹配: /api/** -> ^/api/.*$ 匹配结果: true
[加密调试] 是否需要加密: true
[加密调试] 开始加密请求体，原始数据长度: 45
[加密调试] 加密成功，加密后数据长度: 64
[加密调试] 已添加加密标识头
```

### 3. 检查Network面板

1. 打开浏览器开发者工具 -> Network
2. 执行一个POST请求（如登录）
3. 点击该请求，查看：
   - **Request Headers** 中是否有 `X-Request-Encrypted: true`
   - **Request Payload** 应该是加密后的Base64字符串（不是JSON格式）

### 4. 常见问题排查

#### 问题1: 没有看到任何调试日志
**可能原因：**
- 环境变量未设置或未生效
- 需要重启开发服务器

**解决方法：**
```bash
# 停止服务器，然后重新启动
npm run dev
```

#### 问题2: 看到"加密功能已启用"但没有加密
**可能原因：**
- URL路径不匹配
- 请求方法不是POST或PUT

**检查方法：**
查看控制台日志中的：
- `[加密调试] 请求路径:` - 检查是否匹配配置的URL模式
- `[加密调试] 模式匹配:` - 检查匹配结果是否为true
- `[加密调试] 是否需要加密:` - 应该为true

**解决方法：**
如果URL不匹配，修改 `.env` 文件中的 `VITE_ENCRYPT_URLS`，例如：
```env
# 匹配所有/api开头的路径
VITE_ENCRYPT_URLS=/api/**

# 或者匹配特定路径
VITE_ENCRYPT_URLS=/api/user/**,/api/order/**
```

#### 问题3: 看到"加密成功"但请求头没有标识
**可能原因：**
- 代码执行顺序问题

**解决方法：**
检查代码是否正确执行到添加请求头的部分

#### 问题4: 加密失败错误
**可能原因：**
- crypto-js未正确导入
- 加密函数有错误

**解决方法：**
```bash
# 确保已安装crypto-js
npm install crypto-js

# 检查导入是否正确
# 在 src/utils/aes.js 中应该有: import CryptoJS from 'crypto-js'
```

### 5. 手动测试加密功能

在浏览器控制台运行：

```javascript
// 测试环境变量
console.log('加密启用:', import.meta.env.VITE_ENCRYPT_ENABLED)
console.log('加密URL:', import.meta.env.VITE_ENCRYPT_URLS)

// 测试加密函数（需要先导入）
import { encrypt } from '@/utils/aes'
const testData = '{"username":"test"}'
const encrypted = encrypt(testData)
console.log('加密结果:', encrypted)
```

### 6. 检查URL匹配规则

URL匹配使用正则表达式，支持：
- `/api/**` - 匹配所有以 `/api/` 开头的路径
- `/api/user/*` - 匹配 `/api/user/` 下的单层路径
- `/api/**` 会转换为正则 `^/api/.*$`

**测试URL匹配：**
```javascript
// 在控制台测试
const pattern = '/api/**'
const regexStr = pattern.replace(/\./g, '\\.').replace(/\*\*/g, '.*')
const regex = new RegExp('^' + regexStr + '$')
console.log('匹配 /api/user/login:', regex.test('/api/user/login'))
console.log('匹配 /api/order/list:', regex.test('/api/order/list'))
console.log('匹配 /prod-api/api/user/login:', regex.test('/prod-api/api/user/login'))
```

### 7. 完整检查清单

- [ ] `.env` 文件存在且包含 `VITE_ENCRYPT_ENABLED=true`
- [ ] 开发服务器已重启
- [ ] 浏览器控制台有调试日志输出
- [ ] URL路径匹配成功（日志显示 `匹配结果: true`）
- [ ] 请求方法为 POST 或 PUT
- [ ] Network面板显示请求头 `X-Request-Encrypted: true`
- [ ] 请求体是加密后的Base64字符串
- [ ] crypto-js已安装（`npm list crypto-js`）

## 🚀 快速修复

如果以上都检查过了还是不行，可以尝试：

1. **强制启用加密（临时测试）**

修改 `src/config/http/index.js`，将：
```javascript
const encryptEnabled = import.meta.env.VITE_ENCRYPT_ENABLED === 'true' || import.meta.env.VITE_ENCRYPT_ENABLED === true
```
改为：
```javascript
const encryptEnabled = true  // 强制启用，用于测试
```

2. **简化URL匹配（临时测试）**

将：
```javascript
const encryptUrls = import.meta.env.VITE_ENCRYPT_URLS || '/api/**'
```
改为：
```javascript
const encryptUrls = '/api/**'  // 直接配置，不依赖环境变量
```

3. **检查实际请求URL**

在请求拦截器开始处添加：
```javascript
console.log('完整请求信息:', {
    baseURL: config.baseURL,
    url: config.url,
    method: config.method,
    fullUrl: (config.baseURL || '') + (config.url || '')
})
```

## 📞 如果还是不行

请提供以下信息：
1. 浏览器控制台的完整日志
2. Network面板中请求的详细信息（截图）
3. `.env` 文件内容（隐藏敏感信息）
4. 实际请求的URL路径
