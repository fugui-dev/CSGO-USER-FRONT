# 接口加密对接文档（前端）

> 版本：v1.0  
> 适用项目：所有对接本后端服务的前端项目  
> 加密方案：AES/ECB/PKCS7 + Base64

---

## 一、方案概述

后端对 POST / PUT 请求的请求 Body 进行透明解密，前端只需在发送请求前将 Body 加密，并附带特定请求头，后端过滤器会自动解密，Controller 层无感知。

**当前状态：加密功能默认关闭，可按需启用。**

---

## 二、加密算法参数

| 参数 | 值 |
|---|---|
| 算法 | AES |
| 模式 | ECB |
| 填充 | PKCS7（等同 Java 的 PKCS5Padding） |
| 密钥 | `CSGO2024SecretKey`（16字节，生产环境请向后端确认当前密钥） |
| 输出格式 | **纯 Base64 字符串**（不含 OpenSSL Salted__ 头） |
| 编码 | UTF-8 |

---

## 三、快速接入步骤

### 第一步：安装依赖

```bash
npm install crypto-js
```

### 第二步：创建加密工具文件

新建 `src/utils/aes.js`：

```js
import CryptoJS from 'crypto-js'

// 密钥（必须与后端 AESUtils.java 中的 DEFAULT_KEY 保持一致）
const DEFAULT_KEY = 'CSGO2024SecretKey'

/**
 * AES 加密
 * @param {String} data  待加密字符串（通常为 JSON.stringify 后的对象）
 * @param {String} key   可选，默认使用 DEFAULT_KEY
 * @returns {String}     纯 Base64 字符串
 */
export function encrypt(data, key = DEFAULT_KEY) {
  try {
    const adjustedKey = adjustKey(key)
    const encrypted = CryptoJS.AES.encrypt(
      CryptoJS.enc.Utf8.parse(data),
      CryptoJS.enc.Utf8.parse(adjustedKey),
      {
        mode: CryptoJS.mode.ECB,
        padding: CryptoJS.pad.Pkcs7
      }
    )
    // 必须用 .ciphertext.toString(Base64)，与 Java Base64.getEncoder() 输出兼容
    return encrypted.ciphertext.toString(CryptoJS.enc.Base64)
  } catch (error) {
    console.error('AES加密失败:', error)
    throw new Error('加密失败: ' + error.message)
  }
}

/**
 * AES 解密（前端一般无需调用，仅用于调试验证）
 * @param {String} encryptedData  Base64 加密字符串
 * @param {String} key            可选，默认使用 DEFAULT_KEY
 * @returns {String}              原始字符串
 */
export function decrypt(encryptedData, key = DEFAULT_KEY) {
  try {
    const adjustedKey = adjustKey(key)
    const encryptedWordArray = CryptoJS.enc.Base64.parse(encryptedData)
    const encryptedParams = CryptoJS.lib.CipherParams.create({
      ciphertext: encryptedWordArray
    })
    const decrypted = CryptoJS.AES.decrypt(
      encryptedParams,
      CryptoJS.enc.Utf8.parse(adjustedKey),
      {
        mode: CryptoJS.mode.ECB,
        padding: CryptoJS.pad.Pkcs7
      }
    )
    return decrypted.toString(CryptoJS.enc.Utf8)
  } catch (error) {
    console.error('AES解密失败:', error)
    throw new Error('解密失败: ' + error.message)
  }
}

/**
 * 密钥长度自动调整为 16 / 24 / 32 字节
 * 与后端 AESUtils.adjustKey() 逻辑完全对应
 */
function adjustKey(key) {
  const len = key.length
  if (len === 16 || len === 24 || len === 32) return key
  if (len < 16) return key.padEnd(16, '0').substring(0, 16)
  if (len < 24) return key.substring(0, 16)
  if (len < 32) return key.substring(0, 24)
  return key.substring(0, 32)
}
```

### 第三步：在 axios 请求拦截器中集成加密

```js
import { encrypt } from '@/utils/aes'

instance.interceptors.request.use((config) => {
  // Token 注入
  const token = localStorage.getItem('token')
  if (token) config.headers.Authorization = token

  // ==================== 加密逻辑 ====================
  const encryptEnabled = true // 改为 false 可全局关闭加密

  if (
    encryptEnabled &&
    (config.method === 'post' || config.method === 'put') &&
    config.data
  ) {
    try {
      // 1. 将 data 序列化为 JSON 字符串
      const originalStr =
        typeof config.data === 'string'
          ? config.data
          : JSON.stringify(config.data)

      // 2. 加密
      const encryptedData = encrypt(originalStr)

      // 3. 加密后以纯文本发送，避免被 axios 再次 JSON 序列化
      config.headers['Content-Type'] = 'text/plain;charset=UTF-8'
      config.data = encryptedData

      // 4. 添加加密标识头（后端 DecryptFilter 依赖此头识别加密请求）
      config.headers['X-Request-Encrypted'] = 'true'
    } catch (error) {
      console.error('请求加密失败，降级为明文发送:', error)
      if (typeof config.data !== 'string') {
        config.data = JSON.stringify(config.data)
      }
    }
  } else {
    // 不加密，正常 JSON 序列化
    if (
      (config.method === 'post' || config.method === 'put') &&
      typeof config.data !== 'string'
    ) {
      config.data = JSON.stringify(config.data)
    }
  }
  // ==================== 加密逻辑结束 ====================

  return config
})
```

---

## 四、工作流程

```
前端发起 POST/PUT 请求
        ↓
JSON.stringify(requestBody)
        ↓
AES/ECB/PKCS7 加密 → 纯 Base64 字符串
        ↓
设置 Content-Type: text/plain;charset=UTF-8
设置 X-Request-Encrypted: true
        ↓
HTTP 请求到达后端
        ↓
DecryptFilter 拦截
  ├─ 检测到 X-Request-Encrypted: true → 标记需要解密
  └─ 或 URL 匹配 decrypt-urls 配置 → 标记需要解密
        ↓
DecryptRequestWrapper 读取 Body → Base64 解码 → AES 解密 → 原始 JSON
        ↓
Controller 正常接收 JSON 数据（无感知）
```

---

## 五、后端配置（供参考，无需前端操作）

后端 `application.yml` 相关配置项：

```yaml
encrypt:
  enabled: true           # true=启用解密过滤器，false=透传不解密
  decrypt-urls: /api/**   # 需要解密的 URL 路径（支持 * 和 ** 通配符）
```

> **重要**：只要前端请求头携带了 `X-Request-Encrypted: true`，后端**无论 URL 是否匹配**都会尝试解密，无需额外配置路径。

---

## 六、注意事项

| 事项 | 说明 |
|---|---|
| 加密范围 | 仅 POST / PUT 的请求 Body，GET / DELETE 的 query 参数**不加密** |
| Content-Type | 加密后必须设置为 `text/plain`，否则后端 JSON 解析会报错 |
| 密钥一致性 | 前后端密钥必须完全相同，密钥长度不满足要求时两端均按相同规则截断/补零 |
| 降级策略 | 加密失败时自动降级为明文发送，业务不中断 |
| 生产环境 | 生产环境务必更换默认密钥 `CSGO2024SecretKey`，前后端同步修改 |
| 后端关闭时 | 若后端 `encrypt.enabled=false`，前端发加密请求仍正常（后端透传），但业务数据会被当作加密字符串处理，**需保持开关同步** |

---

## 七、调试与验证

### 方式一：浏览器控制台快速验证

```js
// 在已加载 crypto-js 的页面控制台执行
const key = CryptoJS.enc.Utf8.parse('CSGO2024SecretKey')
const data = '{"test":"hello world"}'
const result = CryptoJS.AES.encrypt(CryptoJS.enc.Utf8.parse(data), key, {
  mode: CryptoJS.mode.ECB,
  padding: CryptoJS.pad.Pkcs7
})
console.log('加密结果:', result.ciphertext.toString(CryptoJS.enc.Base64))
// 将此 Base64 字符串交给后端用 AESUtils.decrypt() 验证
```

### 方式二：加密前后对比

开启加密后，在 Network 面板中：
- 请求头应包含：`X-Request-Encrypted: true`
- 请求头 Content-Type 应为：`text/plain;charset=UTF-8`
- 请求 Body 应为一串 Base64 字符串（非 JSON 格式）

### 常见错误排查

| 现象 | 原因 | 解决方案 |
|---|---|---|
| 后端报 JSON 解析错误 | Content-Type 未改为 text/plain | 确认拦截器中已修改 Content-Type |
| 后端返回"请求解密失败" | 前后端密钥不一致 | 检查两端 DEFAULT_KEY 是否完全相同 |
| 接口 400 / 500 | 加密数据被二次序列化 | 确认 config.data 直接赋值字符串，未再 JSON.stringify |
| 功能异常但无报错 | 后端 enabled=false 但前端已加密 | 保持前后端开关状态同步 |
