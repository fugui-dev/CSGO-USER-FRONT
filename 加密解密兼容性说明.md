# 加密解密兼容性修复说明

## 问题原因

CryptoJS的`encrypted.toString()`默认返回的格式不是纯Base64字符串，而是包含格式信息的字符串，导致Java后端无法正确解密。

## 修复方案

### 前端修复

修改了 `src/utils/aes.js` 中的加密函数：

**修复前：**
```javascript
return encrypted.toString()  // 返回包含格式信息的字符串
```

**修复后：**
```javascript
return encrypted.ciphertext.toString(CryptoJS.enc.Base64)  // 返回纯Base64字符串
```

这样返回的Base64字符串与Java后端的`Base64.getDecoder().decode()`完全兼容。

### 后端改进

改进了错误处理，现在会输出更详细的错误信息，方便调试。

## 测试验证

修复后，请测试：

1. **前端加密测试**（浏览器控制台）：
```javascript
import { encrypt, decrypt } from '@/utils/aes'
const data = '{"test":"value"}'
const encrypted = encrypt(data)
console.log('加密结果:', encrypted)
const decrypted = decrypt(encrypted)
console.log('解密结果:', decrypted)
console.log('是否一致:', data === decrypted)
```

2. **前后端一致性测试**：
   - 前端加密一个字符串
   - 复制加密结果
   - 使用后端解密工具解密
   - 验证结果是否一致

## 注意事项

1. 修复后需要**刷新浏览器**才能生效
2. 如果还有问题，检查浏览器控制台是否有加密错误
3. 检查后端日志中的详细错误信息
