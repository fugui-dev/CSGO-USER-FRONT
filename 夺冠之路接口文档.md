# 夺冠之路接口文档

> 所有 HTTP 接口均需在 Header 中携带 `Authorization: {token}`
>
> 基础路径前缀（前端代理）：`/prod-api`
>
> 接口基础路径：`/api/match`

---

## 一、HTTP 接口

### 1. 获取当前进行中的比赛信息

- **Method**：`GET`
- **Path**：`/api/match/info`
- **Auth**：需要 Token

**响应 `data` 类型**：`MatchDTO`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 比赛ID |
| name | String | 比赛名称 |
| description | String | 比赛描述 |
| dayTime | String | 比赛日期（yyyy-MM-dd） |
| startTime | String | 比赛开始时间 |
| endTime | String | 比赛结束时间 |
| amount | String | 报名费 |
| status | Integer | 比赛状态（0-未开始 1-进行中 2-已结束） |
| maxTeamNum | Integer | 最大队伍数（默认16） |
| teamSize | Integer | 每队人数（默认10） |
| signUpStartTime | String | 报名开始时间 |
| signUpEndTime | String | 报名截止时间 |
| openTime | String | 全服开放时间（报名未满时开放） |
| closeTime | String | 比赛关闭时间（结束后仍可访问的截止时间） |
| createTime | String | 创建时间 |

---

### 2. 获取历史比赛记录

- **Method**：`GET`
- **Path**：`/api/match/history/list`
- **Auth**：需要 Token

**响应 `data` 类型**：`List<MatchDTO>`（字段同上）

---

### 3. 获取比赛队伍列表

- **Method**：`POST`
- **Path**：`/api/match/team/list`
- **Auth**：需要 Token

**请求体**：

```json
{ "matchId": 1 }
```

**响应 `data` 类型**：`List<MatchTeamDTO>`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 队伍ID |
| matchId | Integer | 比赛ID |
| name | String | 队伍名称 |
| description | String | 队伍描述 |
| status | Integer | 队伍状态（0-招募中 1-已组建 2-已解散） |
| memberCount | Integer | 当前成员数 |
| maxMemberCount | Integer | 最大成员数 |
| captainUserId | Integer | 队长用户ID |
| avatar | String | 队伍头像 |
| totalScore | String | 队伍总积分 |
| winCount | Integer | 总胜场数 |
| rank | Integer | 总排名 |
| matchUserList | List | 队伍成员列表（见 MatchUserDTO） |

**MatchUserDTO**：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 记录ID |
| userId | Integer | 用户ID |
| nickName | String | 用户昵称 |
| userAvatar | String | 用户头像 |
| isCaptain | Boolean | 是否队长 |
| totalScore | String | 个人总积分 |
| ornamentId | Long | 获得的饰品ID |
| ornamentName | String | 饰品名称 |
| ornamentImage | String | 饰品图片 |
| ornamentPrice | BigDecimal | 饰品价格 |

---

### 4. 创建队伍

- **Method**：`POST`
- **Path**：`/api/match/team/create`
- **Auth**：需要 Token

**请求体**：

```json
{
  "matchId": 1,
  "name": "队伍名称",
  "description": "队伍描述",
  "avatar": "头像URL"
}
```

**响应**：`data` 为新创建的队伍ID（Integer）

---

### 5. 加入队伍

- **Method**：`POST`
- **Path**：`/api/match/join/match/team`
- **Auth**：需要 Token

**请求体**：

```json
{
  "matchId": 1,
  "teamId": 2
}
```

---

### 6. 邀请用户加入队伍

- **Method**：`POST`
- **Path**：`/api/match/invite/user`
- **Auth**：需要 Token

**请求体**：

```json
{
  "matchId": 1,
  "teamId": 2,
  "invitedUserId": 100,
  "message": "邀请加入队伍"
}
```

---

### 7. 处理邀请（接受/拒绝）

- **Method**：`POST`
- **Path**：`/api/match/invite/deal/with`
- **Auth**：需要 Token

**请求体**：

```json
{
  "id": 10,
  "status": 1
}
```

| status | 说明 |
|--------|------|
| 1 | 接受 |
| 2 | 拒绝 |

---

### 8. 获取邀请/被邀请列表

- **Method**：`POST`
- **Path**：`/api/match/user/invite/list`
- **Auth**：需要 Token

**请求体**：

```json
{
  "matchId": 1,
  "page": 1,
  "size": 10
}
```

**响应 `data` 类型**：`List<MatchUserInviteDTO>`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 邀请ID |
| matchId | Integer | 比赛ID |
| teamId | Integer | 队伍ID |
| teamName | String | 队伍名称 |
| teamAvatar | String | 队伍头像 |
| captainUserId | Integer | 队长ID |
| captainUserName | String | 队长名称 |
| invitedUserId | Integer | 被邀请用户ID |
| invitedUserName | String | 被邀请用户名 |
| status | Integer | 邀请状态（0-未接受 1-已接受 2-已拒绝） |
| inviteTime | String | 邀请时间 |
| message | String | 邀请消息 |

---

### 9. 审核入队申请列表

- **Method**：`POST`
- **Path**：`/api/match/user/examine/list`
- **Auth**：需要 Token

**请求体**：

```json
{
  "matchId": 1,
  "teamId": 2,
  "page": 1,
  "size": 10
}
```

**响应 `data` 类型**：`List<MatchUserExamineDTO>`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 记录ID |
| userId | Integer | 申请用户ID |
| nickName | String | 用户昵称 |
| userAvatar | String | 用户头像 |
| matchId | Integer | 比赛ID |
| teamId | Integer | 申请加入的队伍ID |
| status | Integer | 审核状态（0-待审核 1-已通过 2-已拒绝） |
| opinion | String | 审核意见 |

---

### 10. 审核用户（同意/拒绝入队）

- **Method**：`POST`
- **Path**：`/api/match/examine/user`
- **Auth**：需要 Token

**请求体**：

```json
{
  "id": 5,
  "status": 1,
  "opinion": "欢迎加入"
}
```

---

### 11. 获取阶段列表

- **Method**：`POST`
- **Path**：`/api/match/stage/list`
- **Auth**：需要 Token

**请求体**：

```json
{ "matchId": 1 }
```

**响应 `data` 类型**：`List<StageDTO>`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 阶段ID |
| type | Integer | 阶段类型（1-小组赛 2-淘汰赛8强 3-淘汰赛4强 4-决赛） |
| name | String | 阶段名称 |
| status | Integer | 阶段状态（0-未开始 1-进行中 2-已结束） |
| startTime | String | 开始时间 |
| endTime | String | 结束时间 |
| teamNum | Integer | 该阶段最大队伍数 |
| stageGroupList | List | 分组列表（每组含 groupName、stageGroupFightList） |
| stageTeamList | List | 阶段所有队伍及成员 |

---

### 12. 获取对战初始化数据

- **Method**：`POST`
- **Path**：`/api/match/stage/group/fight/list`
- **Auth**：需要 Token

**请求体**：

```json
{ "stageGroupFightId": 10 }
```

**响应 `data` 类型**：`StageGroupFightStartDTO`（含双方队伍信息、回合列表、当前进度）

---

### 13. 开始回合

- **Method**：`POST`
- **Path**：`/api/match/start/round`
- **Auth**：需要 Token

**请求体**：

```json
{ "stageRecordId": 20 }
```

**响应 `data` 类型**：`StartRoundDTO`（含当前回合信息）

---

### 14. 玩家选择概率

- **Method**：`POST`
- **Path**：`/api/match/select/probability`
- **Auth**：需要 Token

**请求体**：

```json
{
  "stageRecordId": 20,
  "probability": 60
}
```

**响应 `data` 类型**：`StageRecordStartDTO`（含回合状态、双方选择、结果等）

| 字段 | 说明 |
|------|------|
| data | 己方选择的概率 |
| opponentData | 对手选择的概率 |
| resultData | 实际开出的概率 |
| result | 回合结果（1-胜 0-平 -1-负） |
| score / opponentScore | 双方当前得分 |

---

### 15. 结束回合

- **Method**：`POST`
- **Path**：`/api/match/end/round`
- **Auth**：需要 Token

**请求体**：

```json
{ "stageRecordId": 20 }
```

---

### 16. 结束组间对战

- **Method**：`POST`
- **Path**：`/api/match/end/stage/group/fight`
- **Auth**：需要 Token

**请求体**：

```json
{ "stageGroupFightId": 10 }
```

---

### 17. 结束阶段

- **Method**：`POST`
- **Path**：`/api/match/end/stage`
- **Auth**：需要 Token

**请求体**：

```json
{ "stageId": 3 }
```

---

### 18. 获取助威配置列表

- **Method**：`GET`
- **Path**：`/api/match/cheer/config/list`
- **Auth**：需要 Token

**响应 `data` 类型**：`List<StageCheerConfigDTO>`（助威档位、金额、倍数、积分比例等）

---

### 19. 助威

- **Method**：`POST`
- **Path**：`/api/match/cheer`
- **Auth**：需要 Token

**请求体**：

```json
{
  "matchId": 1,
  "stageGroupFightId": 10,
  "teamId": 2,
  "amount": "10.00"
}
```

---

### 20. 获取助威列表

- **Method**：`POST`
- **Path**：`/api/match/cheer/list`
- **Auth**：需要 Token

**请求体**：

```json
{
  "matchId": 1,
  "stageGroupFightId": 10,
  "teamId": 2
}
```

**响应 `data` 类型**：`List<StageCheerDTO>`

| 字段 | 类型 | 说明 |
|------|------|------|
| userId | Integer | 助威用户ID |
| userName | String | 用户名称 |
| userAvatar | String | 用户头像 |
| teamId | Integer | 助威队伍ID |
| teamName | String | 队伍名称 |
| amount | String | 助威金额 |
| refundAmount | String | 退款金额 |

---

### 21. 获取用户可领取的奖励列表

- **Method**：`GET`
- **Path**：`/api/match/reward/list`
- **Auth**：需要 Token

**响应 `data` 类型**：`List<MatchRewardDTO>`

| 字段 | 类型 | 说明 |
|------|------|------|
| rewardType | Integer | 奖励类型（1-冠军宝箱 2-亚军宝箱 3-淘汰奖励宝箱） |
| rewardTypeName | String | 奖励类型名称 |
| boxId | Integer | 宝箱ID |
| boxName | String | 宝箱名称 |
| boxImage | String | 宝箱图片 |
| canClaim | Boolean | 是否可领取 |
| claimed | Boolean | 是否已领取 |
| boxRecordId | Long | 开箱记录ID（已领取时有值） |

---

### 22. 领取奖励（开箱）

- **Method**：`POST`
- **Path**：`/api/match/reward/claim`
- **Auth**：需要 Token

**请求体**：

```json
{ "rewardType": 1 }
```

| rewardType | 说明 |
|------------|------|
| 1 | 冠军宝箱 |
| 2 | 亚军宝箱 |
| 3 | 淘汰奖励宝箱 |

**响应 `data` 类型**：`List<OpenBoxVO>`（开箱结果，含饰品信息）

---

## 二、WebSocket 接口

### 消息通用格式

**服务端推送格式**：

```json
{
  "code": 200,
  "typeName": "消息类型",
  "data": { ... },
  "timestamp": 1712000000000
}
```

**客户端发送格式（FightWS）**：

```json
{
  "type": "消息类型",
  "data": { ... }
}
```

---

### WS1：阶段全局推送

- **连接地址**：`ws://host/websocket/stage/{userId}`
- **说明**：推送阶段全局事件，所有在线用户共享，进入比赛页面即连接

#### 客户端可发送的消息

| type | data | 说明 |
|------|------|------|
| `GET_STAGE_INFO` | `{ "matchId": 1 }` | 主动查询当前阶段信息 |
| `ping` | — | 心跳，服务端回 `pong` |

#### 服务端推送消息详情

---

**`STAGE_UPDATE`** — 阶段数据更新

触发时机：阶段开始时推送

data 类型：`List<StageDTO>`（各阶段完整信息，含分组和队伍）

```json
{
  "typeName": "STAGE_UPDATE",
  "data": {
    "code": 200,
    "data": [
      {
        "id": 1,
        "type": 1,
        "name": "小组赛",
        "status": 1,
        "startTime": "2026-04-01 10:00:00",
        "endTime": "2026-04-01 18:00:00",
        "teamNum": 16,
        "stageGroupList": [
          {
            "id": 10,
            "stageId": 1,
            "groupName": "A",
            "teamCount": 4,
            "stageGroupFightList": [ ... ]
          }
        ],
        "stageTeamList": [ ... ]
      }
    ]
  }
}
```

---

**`STAGE_END`** — 阶段结束

触发时机：阶段结束时推送

data 类型：同 `STAGE_UPDATE`（最新阶段列表）

---

**`GET_STAGE_INFO`** — 查询阶段信息响应

触发时机：客户端发送 `GET_STAGE_INFO` 后的响应

data 同 `STAGE_UPDATE`

---

**`ERROR`** — 错误通知

```json
{ "typeName": "ERROR", "data": "错误信息描述" }
```

---

**`pong`** — 心跳响应

```json
{ "typeName": "pong", "data": "pong" }
```

---

### WS2：对战房间实时推送

- **连接地址**：`ws://host/ws/fight/{fightId}/{userId}`
- **说明**：进入对战房间时连接，推送回合级实时消息；连接成功后服务端立即推送一次当前对战进度

#### 客户端可发送的消息

| type | data 字段 | 说明 |
|------|-----------|------|
| `START_FIGHT` | `{ "stageGroupFightId": 10 }` | 开始对战 |
| `START_ROUND` | `{ "stageRecordId": 20 }` | 开始回合（己方准备好） |
| `SELECT_PROBABILITY` | `{ "stageRecordId": 20, "probabilitySelect": 60 }` | 选择概率（1-100） |
| `END_ROUND` | `{ "stageRecordId": 20 }` | 结束回合 |
| `END_FIGHT` | `{ "stageGroupFightId": 10 }` | 结束对战 |
| `GET_CURRENT_PROGRESS` | `fightId`（数字） | 主动拉取当前对战进度 |
| `ping` | — | 心跳 |

#### 服务端推送消息详情

---

**`CURRENT_PROGRESS`** — 连接后立即推送当前对战进度

触发时机：客户端连接成功时自动推送

data 类型：`StageGroupFightStartDTO`

```json
{
  "typeName": "CURRENT_PROGRESS",
  "data": {
    "id": 10,
    "stageId": 1,
    "groupName": "A",
    "teamId": 2,
    "teamName": "红队",
    "teamAvatar": "https://...",
    "opponentTeamId": 3,
    "opponentTeamName": "蓝队",
    "opponentTeamAvatar": "https://...",
    "round": 3,
    "recordRound": 2,
    "status": 1,
    "startTime": "2026-04-01 10:00:00",
    "endTime": null,
    "team": {
      "teamId": 2,
      "initialScore": "10",
      "cheerAmount": "50.00",
      "aliasName": "A队",
      "stageRecordStartList": [
        {
          "stageId": 1,
          "userId": 100,
          "userName": "玩家A",
          "opponentUserId": 200,
          "round": 1,
          "totalRound": 5,
          "score": "5",
          "opponentScore": "3",
          "totalScore": "25",
          "result": 1,
          "status": 2
        }
      ]
    },
    "opponentTeam": { ... }
  }
}
```

---

**`FIGHT_START`** — 对战开始

触发时机：`END_FIGHT` 处理成功时广播（即双方确认开始）

data 类型：`StageGroupFight`（对战组实体，同 `CURRENT_PROGRESS` 的 `StageGroupFightStartDTO`）

```json
{
  "typeName": "FIGHT_START",
  "data": {
    "id": 10,
    "stageId": 1,
    "groupName": "A",
    "status": 1,
    "startTime": "2026-04-01 10:00:00"
  }
}
```

---

**`ROUND_START`** — 回合开始

触发时机：任一玩家调用 `START_ROUND` 后广播给双方

data 类型：`StartRoundDTO`

```json
{
  "typeName": "ROUND_START",
  "data": {
    "stageRecordId": 20,
    "isFight": true,
    "userId": 100,
    "opponentUserId": 200,
    "userStatus": 1,
    "opponentUserStatus": 1,
    "startTime": "2026-04-01 10:05:00",
    "endTime": null
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| stageRecordId | Integer | 本人的回合记录ID |
| isFight | Boolean | `true`=双方都已准备好可以战斗，`false`=等待对手准备 |
| userId | Integer | 己方用户ID |
| opponentUserId | Integer | 对手用户ID |
| userStatus | Integer | 己方状态（1=进行中） |
| opponentUserStatus | Integer | 对手状态 |
| startTime | String | 回合开始时间 |

---

**`ROUND_WAITING`** — 等待对手选择概率

触发时机：己方已选择概率，等待对手选择

data 类型：`StageRecordStartDTO`（含己方已选的信息，对手 `opponentData` 为 null）

```json
{
  "typeName": "ROUND_WAITING",
  "data": {
    "stageRecordId": 20,
    "userId": 100,
    "data": 60,
    "opponentData": null,
    "resultData": null,
    "score": null,
    "result": null,
    "status": 1
  }
}
```

---

**`ROUND_RESULT`** — 回合结果（双方都已选择）

触发时机：双方均已选择概率，广播最终结果

data 类型：`StageRecordStartDTO`

```json
{
  "typeName": "ROUND_RESULT",
  "data": {
    "stageRecordId": 20,
    "round": 1,
    "totalRound": 5,
    "userId": 100,
    "opponentUserId": 200,
    "data": 60,
    "opponentData": 45,
    "resultData": 55,
    "score": "5",
    "opponentScore": "3",
    "totalScore": "15",
    "opponentTotalScore": "10",
    "result": 1,
    "opponentResult": 0,
    "status": 2,
    "startTime": "2026-04-01 10:05:00",
    "endTime": "2026-04-01 10:05:30"
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| data | Integer | 己方选择的概率（1-100） |
| opponentData | Integer | 对手选择的概率 |
| resultData | Integer | 实际开出的随机概率 |
| result | Integer | 己方回合结果（1-胜 0-负） |
| opponentResult | Integer | 对手回合结果 |
| score | String | 己方本回合得分 |
| opponentScore | String | 对手本回合得分 |
| totalScore | String | 己方累计总分 |
| opponentTotalScore | String | 对手累计总分 |
| isCaptain | Boolean | 己方是否为队长（队长得分有加成） |

---

**`FIGHT_END`** — 对战结束

触发时机：`END_FIGHT` 处理完成后广播

data 类型：`StageGroupFight`（含最终状态）

```json
{
  "typeName": "FIGHT_END",
  "data": {
    "id": 10,
    "stageId": 1,
    "groupName": "A",
    "status": 2,
    "startTime": "2026-04-01 10:00:00",
    "endTime": "2026-04-01 10:30:00"
  }
}
```

---

**`ERROR`** — 错误通知

```json
{ "typeName": "ERROR", "data": "错误信息描述" }
```

---

**`pong`** — 心跳响应

```json
{ "typeName": "pong", "data": "pong" }
```

---

### WS 消息类型速查表

#### WS1（阶段全局 `/websocket/stage/{userId}`）

| 方向 | typeName | 触发时机 | data 类型 |
|------|----------|----------|-----------|
| 服务端→客户端 | `STAGE_UPDATE` | 阶段开始/更新 | List\<StageDTO\> |
| 服务端→客户端 | `STAGE_END` | 阶段结束 | List\<StageDTO\> |
| 服务端→客户端 | `GET_STAGE_INFO` | 查询阶段响应 | List\<StageDTO\> |
| 服务端→客户端 | `ERROR` | 处理异常 | String |
| 服务端→客户端 | `pong` | 心跳响应 | — |
| 客户端→服务端 | `GET_STAGE_INFO` | 主动查询 | `{ matchId }` |
| 客户端→服务端 | `ping` | 心跳 | — |

#### WS2（对战房间 `/ws/fight/{fightId}/{userId}`）

| 方向 | typeName | 触发时机 | data 类型 |
|------|----------|----------|-----------|
| 服务端→客户端 | `CURRENT_PROGRESS` | 连接成功后立即推送 | StageGroupFightStartDTO |
| 服务端→客户端 | `FIGHT_START` | 对战开始 | StageGroupFight |
| 服务端→客户端 | `ROUND_START` | 任一玩家发起回合 | StartRoundDTO |
| 服务端→客户端 | `ROUND_WAITING` | 己方已选，等待对手 | StageRecordStartDTO |
| 服务端→客户端 | `ROUND_RESULT` | 双方均已选择概率 | StageRecordStartDTO |
| 服务端→客户端 | `FIGHT_END` | 对战结束 | StageGroupFight |
| 服务端→客户端 | `ERROR` | 处理异常 | String |
| 服务端→客户端 | `pong` | 心跳响应 | — |
| 客户端→服务端 | `START_FIGHT` | 开始对战 | `{ stageGroupFightId }` |
| 客户端→服务端 | `START_ROUND` | 开始回合（准备好） | `{ stageRecordId }` |
| 客户端→服务端 | `SELECT_PROBABILITY` | 选择概率 | `{ stageRecordId, probabilitySelect }` |
| 客户端→服务端 | `END_ROUND` | 结束回合 | `{ stageRecordId }` |
| 客户端→服务端 | `END_FIGHT` | 结束对战 | `{ stageGroupFightId }` |
| 客户端→服务端 | `GET_CURRENT_PROGRESS` | 主动拉取进度 | fightId（数字） |
| 客户端→服务端 | `ping` | 心跳 | — |

---

## 三、快速对照表

| 序号 | 功能 | Method | Path | Auth |
|------|------|--------|------|------|
| 1 | 获取当前比赛信息 | GET | `/api/match/info` | 需要 |
| 2 | 获取历史比赛记录 | GET | `/api/match/history/list` | 需要 |
| 3 | 获取队伍列表 | POST | `/api/match/team/list` | 需要 |
| 4 | 创建队伍 | POST | `/api/match/team/create` | 需要 |
| 5 | 加入队伍 | POST | `/api/match/join/match/team` | 需要 |
| 6 | 邀请用户 | POST | `/api/match/invite/user` | 需要 |
| 7 | 处理邀请 | POST | `/api/match/invite/deal/with` | 需要 |
| 8 | 邀请列表 | POST | `/api/match/user/invite/list` | 需要 |
| 9 | 审核申请列表 | POST | `/api/match/user/examine/list` | 需要 |
| 10 | 审核用户 | POST | `/api/match/examine/user` | 需要 |
| 11 | 获取阶段列表 | POST | `/api/match/stage/list` | 需要 |
| 12 | 获取对战初始化数据 | POST | `/api/match/stage/group/fight/list` | 需要 |
| 13 | 开始回合 | POST | `/api/match/start/round` | 需要 |
| 14 | 选择概率 | POST | `/api/match/select/probability` | 需要 |
| 15 | 结束回合 | POST | `/api/match/end/round` | 需要 |
| 16 | 结束组间对战 | POST | `/api/match/end/stage/group/fight` | 需要 |
| 17 | 结束阶段 | POST | `/api/match/end/stage` | 需要 |
| 18 | 助威配置列表 | GET | `/api/match/cheer/config/list` | 需要 |
| 19 | 助威 | POST | `/api/match/cheer` | 需要 |
| 20 | 助威列表 | POST | `/api/match/cheer/list` | 需要 |
| 21 | 获取奖励列表 | GET | `/api/match/reward/list` | 需要 |
| 22 | 领取奖励 | POST | `/api/match/reward/claim` | 需要 |
| WS1 | 阶段全局推送 | WS | `/websocket/stage/{userId}` | — |
| WS2 | 对战房间推送 | WS | `/ws/fight/{fightId}/{userId}` | — |
