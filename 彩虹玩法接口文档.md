# 彩虹玩法 接口文档

> 服务基础路径：`/api/rainbow`  
> 所有 HTTP 接口需在请求头携带 Token：`Authorization: Bearer <token>`  
> 响应体统一格式：`{ "code": 200, "msg": "ok", "data": <数据> }`

---

## 一、枚举值说明

### 游戏状态 status
| 值 | 含义 |
|----|------|
| 0 | 等待下注（倒计时进行中） |
| 1 | 开奖中（不可下注） |
| 2 | 已结算 |

### 物品 item
| 值 | 名称 | 图标 |
|----|------|------|
| 1 | 蓝色圆球 | 🔵 |
| 2 | 紫色圆柱 | 🟣 |
| 3 | 红色三角 | 🔺 |
| 4 | 黄色正方 | 🟡 |

### 颜色 color
| 值 | 名称 | 色值 |
|----|------|------|
| 1 | 紫色 | #9B59B6 |
| 2 | 红色 | #E74C3C |
| 3 | 黄色 | #F39C12 |

### 12种结果倍率对照
| item\color | 紫(1) | 红(2) | 黄(3) |
|-----------|-------|-------|-------|
| 蓝球(1) | 5× | 6× | 7× |
| 紫柱(2) | 8× | 9× | 11× |
| 红三角(3) | 17× | 21× | 25× |
| 黄方(4) | 27× | 33× | 44× |

### 合法下注金额档位
`5 / 10 / 20 / 50 / 100 / 200 / 500 / 1000 / 2000 / 5000`

---

## 二、HTTP 接口

### 1. 获取当前游戏状态

```
GET /api/rainbow/current
```

**说明：** 页面初始化时调用一次，获取当前局完整状态（含倒计时、下注选项、我的下注、近期历史）。后续状态更新由 WebSocket 实时推送，**不需要轮询此接口**。

**请求参数：** 无（userId 从 Token 自动解析）

**响应示例：**
```json
{
  "code": 200,
  "data": {
    "gameId": 318,
    "gameNo": "NO.000318",
    "status": 0,
    "countdownSeconds": 19,
    "resultItem": null,
    "resultColor": null,
    "resultMultiplier": null,
    "betOptions": [
      {
        "item": 1,
        "color": 1,
        "multiplier": 5,
        "betCount": 3,
        "betTotal": 150
      }
      // ... 共12条
    ],
    "myBets": [
      {
        "betItem": 1,
        "betColor": 1,
        "betMultiplier": 5,
        "betAmount": 100
      }
    ],
    "recentResults": [
      {
        "gameNo": "NO.000317",
        "resultItem": 2,
        "resultColor": 1,
        "resultMultiplier": 8,
        "createTime": "2026-04-01 12:30:45"
      }
      // ... 最多10条，从新到旧
    ]
  }
}
```

**字段说明：**
| 字段 | 类型 | 说明 |
|------|------|------|
| gameId | Long | 游戏局唯一ID，下注/撤注时使用 |
| gameNo | String | 游戏编号，展示用（如 NO.000318） |
| status | Integer | 0=下注中 1=开奖中 2=已结算 |
| countdownSeconds | Integer | 剩余下注秒数，仅 status=0 时有效 |
| resultItem | Integer | 开奖物品，仅 status=2 时有值 |
| resultColor | Integer | 开奖颜色，仅 status=2 时有值 |
| resultMultiplier | BigDecimal | 开奖倍率，仅 status=2 时有值 |
| betOptions | Array | 12种结果的当前下注汇总 |
| myBets | Array | 本局当前用户的下注列表，未登录时为 null |
| recentResults | Array | 近10场开奖历史，从新到旧 |

---

### 2. 下注

```
POST /api/rainbow/bet
Content-Type: application/json
```

**请求体：**
```json
{
  "betItem": 1,
  "betColor": 2,
  "betAmount": 100
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| betItem | Integer | 是 | 下注物品 1-4 |
| betColor | Integer | 是 | 下注颜色 1-3 |
| betAmount | BigDecimal | 是 | 下注金额，必须为合法档位 |

**成功响应：**
```json
{ "code": 200, "msg": "ok", "data": "下注成功" }
```

**错误响应示例：**
```json
{ "code": 500, "msg": "余额不足" }
{ "code": 500, "msg": "当前不在下注阶段，请等待下一局" }
{ "code": 500, "msg": "下注时间已截止" }
{ "code": 500, "msg": "下注金额不合法，请选择有效档位" }
```

**注意：** 下注成功后，后端会主动通过 WebSocket 广播 `bet_update` 消息，所有在线用户的下注统计会自动刷新，**前端无需再次请求 `/current`**。

---

### 3. 撤销本局下注

```
POST /api/rainbow/cancelBet?gameId=318
```

**请求参数：**
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| gameId | Long | 是 | 要撤注的游戏局ID |

**成功响应：**
```json
{ "code": 200, "data": 150.00 }
```
> data 为退还金额

**错误响应示例：**
```json
{ "code": 500, "msg": "当前游戏局不支持取消下注" }
{ "code": 500, "msg": "当前局没有下注记录" }
```

**注意：** 仅 status=0（倒计时阶段）可撤注，同样会触发 `bet_update` 广播。

---

### 4. 掉落预览

```
GET /api/rainbow/dropPreview?amount=100
```

**请求参数：**
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| amount | BigDecimal | 5 | 下注金额 |

**响应示例：**
```json
{
  "code": 200,
  "data": [
    { "item": 1, "color": 1, "multiplier": 5, "reward": 500.00 },
    { "item": 1, "color": 2, "multiplier": 6, "reward": 600.00 }
    // ... 共12条
  ]
}
```

---

### 5. 遗漏统计

```
GET /api/rainbow/miss
```

**说明：** 查询12种结果各自距上次出现已经过了多少局（遗漏局数）。

**响应示例：**
```json
{
  "code": 200,
  "data": [
    { "item": 1, "color": 1, "missCount": 3 },
    { "item": 1, "color": 2, "missCount": 0 }
    // ... 共12条
  ]
}
```

---

### 6. 近10场开奖历史

```
GET /api/rainbow/history
```

**响应示例：**
```json
{
  "code": 200,
  "data": [
    {
      "gameNo": "NO.000317",
      "resultItem": 2,
      "resultColor": 1,
      "resultMultiplier": 8,
      "createTime": "2026-04-01 12:30:45"
    }
    // ... 最多10条，从新到旧
  ]
}
```

---

### 7. 我的下注记录（分页）

```
GET /api/rainbow/records?pageNum=1&pageSize=20
```

**请求参数：**
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| pageNum | Integer | 1 | 页码 |
| pageSize | Integer | 20 | 每页条数 |

**响应示例：**
```json
{
  "code": 200,
  "rows": [
    {
      "id": 1001,
      "gameId": 317,
      "gameNo": "NO.000317",
      "betItem": 1,
      "betColor": 2,
      "betMultiplier": 6,
      "betAmount": 100,
      "isWin": 0,
      "winAmount": 0,
      "resultItem": 2,
      "resultColor": 1,
      "createTime": "2026-04-01 12:28:00"
    }
  ],
  "total": 56
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| betItem/betColor | Integer | 下注的物品和颜色 |
| betMultiplier | BigDecimal | 下注时该格的倍率 |
| betAmount | BigDecimal | 下注金额 |
| isWin | Integer | 0=未中奖 1=中奖 |
| winAmount | BigDecimal | 实际获得金额（含本金）|
| resultItem/resultColor | Integer | 本局开奖结果 |

---

## 三、WebSocket 实时推送

### 连接地址
```
ws://<host>/ws/rainbow/{userId}
```
> 未登录游客传 `0`

### 心跳保活
客户端每 30s 发送文本 `ping`，服务端回复 `pong`。

---

### 消息格式（服务端 → 客户端）

所有消息均为 JSON 文本，通过 `type` 字段区分类型。

---

#### 消息类型一览

| type | 触发时机 | 前端动作 |
|------|----------|----------|
| `game_state` | 新局创建(status=0) / 进入开奖中(status=1) | 更新倒计时、下注选项；status=1时触发转盘旋转 |
| `bet_update` | 有人下注或撤注 | 实时刷新各选项的人数和金额 |
| `settle_ready` | 开奖结果已算出（status变2之前） | 触发转盘定位减速动画 |
| `settle_result` | settle_ready 后 6 秒 | 更新UI显示结果、弹出中奖Toast、展示本局总投入/总派奖；5秒后新局开始 |

---

#### `game_state` — 游戏状态变更

```json
{
  "type": "game_state",
  "status": 0,
  "gameId": 318,
  "gameNo": "NO.000318",
  "countdownSeconds": 25,
  "betOptions": [
    { "item": 1, "color": 1, "multiplier": 5, "betCount": 0, "betTotal": 0 }
    // ... 共12条
  ]
}
```

| 字段 | 说明 |
|------|------|
| status=0 | 新局开始，倒计时开始，开放下注 |
| status=1 | 进入开奖中，前端触发**转盘持续旋转动画** |
| countdownSeconds | 仅 status=0 时有值，前端以此值启动本地倒计时 |
| betOptions | 当前各选项下注统计 |

**前端处理逻辑：**
- status=0：同步倒计时，启动本地定时器
- status=1：停止倒计时，**触发转盘旋转动画**（此时等待 `settle_ready`）

---

#### `bet_update` — 下注实时刷新

```json
{
  "type": "bet_update",
  "gameId": 318,
  "betOptions": [
    { "item": 1, "color": 1, "multiplier": 5, "betCount": 3, "betTotal": 150 }
    // ... 共12条
  ]
}
```

**前端处理逻辑：** 校验 `gameId` 与当前局一致后，更新 `betOptions`（人数、金额刷新）。

---

#### `settle_ready` — 触发转盘定位动画

```json
{
  "type": "settle_ready",
  "gameId": 318,
  "gameNo": "NO.000318",
  "resultItem": 2,
  "resultColor": 1,
  "resultMultiplier": 8
}
```

**前端处理逻辑：**
- 触发转盘**减速停止动画**，外圈停到 `resultItem` 对应格，指针停到 `resultColor` 对应格
- 中心区域**仍显示"开奖中"**，不展示结果
- 动画约 4s 完成，后端在 6s 后才推 `settle_result`，时间上有充裕

---

#### `settle_result` — 结算完成展示结果

```json
{
  "type": "settle_result",
  "status": 2,
  "gameId": 318,
  "gameNo": "NO.000318",
  "resultItem": 2,
  "resultColor": 1,
  "resultMultiplier": 8,
  "totalBetAmount": 1500.00,
  "totalWinAmount": 800.00,
  "winAmounts": {
    "101": 800.00,
    "205": 400.00
  },
  "winOrnaments": {
    "101": [
      {
        "betAmount": 100.00,
        "betCount": 2,
        "ornamentName": "AK-47 | 红线 (久经沙场)",
        "ornamentImg": "https://example.com/ak47.png",
        "ornamentPrice": 45.50,
        "boxRecordId": 1001
      },
      {
        "betAmount": 500.00,
        "betCount": 1,
        "ornamentName": "M4A4 | 龙王 (略有磨损)",
        "ornamentImg": "https://example.com/m4a4.png",
        "ornamentPrice": 320.00,
        "boxRecordId": 1003
      }
    ],
    "205": [
      {
        "betAmount": 50.00,
        "betCount": 1,
        "ornamentName": "USP-S | 杀手 (崭新出厂)",
        "ornamentImg": "https://example.com/usp.png",
        "ornamentPrice": 88.00,
        "boxRecordId": 1004
      }
    ]
  },
  "recentResults": [
    {
      "gameNo": "NO.000318",
      "resultItem": 2,
      "resultColor": 1,
      "resultMultiplier": 8,
      "createTime": "2026-04-01 12:30:45"
    }
    // ... 近10场，从新到旧
  ]
}
```

**字段说明：**
| 字段 | 类型 | 说明 |
|------|------|------|
| totalBetAmount | BigDecimal | 本局所有玩家**总下注金额** |
| totalWinAmount | BigDecimal | 本局所有中奖者**总派奖金额**（含本金） |
| winAmounts | Object | 本局中奖明细，key=userId(字符串)，value=该用户获得总金额（含本金）。未中奖用户不在其中 |
| winOrnaments | Object | 本局中奖饰品明细，key=userId(字符串)，value=该用户获得的饰品列表（按下注金额档位分组）。未中奖用户不在其中 |
| winOrnaments[uid][].betAmount | BigDecimal | 该条对应的下注金额档位 |
| winOrnaments[uid][].betCount | Integer | 该档位下注笔数（下了几笔就获得几个该饰品） |
| winOrnaments[uid][].ornamentName | String | 奖励饰品名称 |
| winOrnaments[uid][].ornamentImg | String | 奖励饰品图片URL |
| winOrnaments[uid][].ornamentPrice | BigDecimal | 奖励饰品价格 |
| winOrnaments[uid][].boxRecordId | Long | 背包记录ID（tt_box_records.id），可直接用于调用分解接口 |

> **奖励机制说明：** 中奖不再返还金币，改为发放饰品到背包。不同下注金额对应不同奖励饰品（由管理端配置，共12种结果 × 10种金额档位 = 120种映射）。同一用户若在多个金额档位都下注且中奖，`winOrnaments` 列表中会有多条；同一档位下了多笔则 `betCount > 1`，背包中会收到对应数量的饰品。

**前端处理逻辑：**
1. 更新 `currentGame`（status=2，填充 resultItem/resultColor/resultMultiplier）
2. 更新开奖记录横条（recentResults）
3. 从 `winOrnaments[String(currentUserId)]` 取本人获得的饰品列表，非空则弹中奖 Toast
4. Toast 展示首个饰品的图片/名称/价格；若有多条（多档位）则额外提示「共 N 个，详见背包」
5. 汇总所有档位的 `boxRecordId`，存入本地状态，可直接传给分解接口（`packSackIds`）使用
6. 展示本局统计：`totalBetAmount`（总投入）、`totalWinAmount`（总派奖参考值）

---

### 完整时序图

```
T+0s    后端创建新局
        → [game_state status=0]  前端：倒计时25s开始，开放下注

T+25s   后端推进开奖中
        → [game_state status=1]  前端：触发转盘持续旋转

T+25s   后端结算完成（计算结果写DB）
        → [settle_ready]         前端：触发转盘减速定位动画（~4s）

T+31s   后端广播最终结果
        → [settle_result]        前端：更新UI、弹Toast、展示本局总投入/总派奖（此时动画已停稳）

T+36s   后端创建下一局（5s展示间隔）
        → [game_state status=0]  前端：新局开始，循环
```

> 每轮总时长：**36秒**（25s下注 + 6s动画 + 5s展示）

---

### 中途连入处理

用户连接 WebSocket 时，**服务端会立即单播**当前局完整状态（`game_state` 消息），包含：
- 当前 status 和剩余倒计时
- 当前 betOptions

若 status=2（刚结算完），会额外携带结果字段和历史，前端直接渲染结算状态即可。

---

## 四、前端对接注意事项

1. **页面打开时**：先调 `GET /current` 初始化状态，再建立 WebSocket 连接（WS 连接成功后服务端会再次单播，会覆盖一次，属正常）

2. **中奖判断**：在收到 `game_state(status=1)` 时，**快照保存本局 `myBets`**，等 `settle_result` 到来时从 `winOrnaments[String(currentUserId)]` 取本人饰品列表，非空即中奖。不要在 `settle_result` 时再请求接口查

3. **倒计时**：以 `game_state(status=0)` 中的 `countdownSeconds` 为准启动本地定时器（每秒 -1），后续广播会以服务端为准校正

4. **不要重复请求**：下注/撤注成功后 **不要** 再调 `/current`，等 `bet_update` 广播即可

5. **gameId 校验**：处理 `bet_update` 时需校验 `msg.gameId === currentGame.gameId`，防止新旧局消息串乱
