# 四需求功能接口文档

> 所有接口均需在 Header 中携带 `Authorization: {token}`
> 
> 基础路径前缀（前端代理）：`/prod-api`

---

## 一、幸运升级 - 直接升级（无需背包饰品）

### 接口信息

| 字段 | 内容 |
|------|------|
| 方法 | `POST` |
| 路径 | `/api/luckyUpgrade/upgrade` |
| 认证 | 必须登录 |

### 请求体（application/json）

```json
{
  "upgradeOrnamentId": 123,
  "price": 50.00,
  "probability": 60
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| upgradeOrnamentId | Long | ✅ | 目标饰品ID |
| price | BigDecimal | ✅ | 消费金额，必须大于0 |
| probability | Integer | ✅ | 幸运比例，范围 1~100 |

> **注意**：`packageOrnamentId` 字段不传（只有 upgrade2 需要）

### 成功响应

```json
{
  "code": 200,
  "msg": "ok",
  "data": {
    "ornamentName": "AK-47 | 红线",
    "cover": "https://...",
    "price": 285.00,
    "isWin": 1
  }
}
```

### 错误响应示例

```json
{ "code": 500, "msg": "消费金额必须大于0" }
{ "code": 500, "msg": "余额不足!" }
{ "code": 401, "msg": "登录过期，请重新登录。" }
{ "code": 500, "msg": "网站维护中......" }
{ "code": 500, "msg": "访问频繁，请重试！" }
{ "code": 500, "msg": "为防止机器人恶意注册，请充值后开启玩法！" }
```

---

## 二、对战 - 十佳记录

> 对战最大人数后端已改为 3 人（`playerNumber` 传 2 或 3）。

### 接口信息

| 字段 | 内容 |
|------|------|
| 方法 | `GET` |
| 路径 | `/api/fight/myTopTen` |
| 认证 | 必须登录 |

### 请求参数

无

### 成功响应

```json
{
  "code": 200,
  "data": [
    {
      "currentRound": null,
      "winnerIds": [123, 456],
      "fight": {
        "id": 1001,
        "model": "0",
        "playerNum": 3,
        "seats": "[...]",
        "winnerIds": "123,456",
        "boxPriceTotal": 500.00,
        "status": 2,
        "createTime": "2026-04-08 19:00:00",
        "endTime": "2026-04-08 19:10:00"
      },
      "fightResult": [
        {
          "id": 88001,
          "userId": 123,
          "ornamentName": "AK-47 | 红线",
          "ornamentsPrice": 285.50,
          "imageUrl": "https://...",
          "source": 2,
          "fightId": 1001
        }
      ],
      "fightBoxVOList": [
        {
          "boxId": 10,
          "boxName": "精英箱",
          "boxPrice": 10.00,
          "boxImg": "https://..."
        }
      ]
    }
  ]
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| winnerIds | List\<Integer\> | 赢家用户ID列表 |
| fight | Object | 对战完整信息（含 model、playerNum、status、createTime、endTime 等） |
| fight.model | String | 对战模式：`"0"`=欧皇，`"1"`=非酋 |
| fight.winnerIds | String | 赢家ID（逗号分隔字符串）|
| fight.boxPriceTotal | BigDecimal | 该局箱子总价值 |
| fightResult | List | 该局所有开箱记录（含各玩家饰品） |
| fightBoxVOList | List | 宝箱详情列表 |

> 最多返回 10 条，按本人获得饰品总价值降序  
> 判断是否获胜：`winnerIds.includes(currentUserId)`

### 错误响应示例

```json
{ "code": 401, "msg": "登录过期，请重新登录。" }
```

---

## 三、背包 - 分解记录

### 接口信息

| 字段 | 内容 |
|------|------|
| 方法 | `POST` |
| 路径 | `/api/userPackSack/decomposeLog` |
| 认证 | 必须登录 |

### 请求体（application/json）

```json
{
  "page": 1,
  "size": 10
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | Integer | ✅ | 页码，最小值 1 |
| size | Integer | ✅ | 每页数量，范围 1~20 |

> `userId` 和 `boxRecordStatus` 后端自动填充，前端不传

### 成功响应（分页）

```json
{
  "rows": [
    {
      "boxRecordId": 1001,
      "ornamentName": "AK-47 | 红线",
      "ornamentImgUrl": "https://...",
      "ornamentLevelImg": "https://...",
      "ornamentsPrice": 35.00,
      "boxName": "幸运宝箱",
      "status": "RESOLVE",
      "createTime": "2026-01-01 10:00:00"
    }
  ],
  "total": 50
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| boxRecordId | Long | 记录ID |
| ornamentName | String | 饰品名称 |
| ornamentImgUrl | String | 饰品图片URL |
| ornamentLevelImg | String | 饰品品质图标URL |
| ornamentsPrice | BigDecimal | 分解所得金额（币） |
| boxName | String | 来源箱子名称 |
| status | String | 状态（固定为 `RESOLVE`） |
| createTime | String | 分解时间 |

### 错误响应示例

```json
{ "code": 401, "msg": "登录过期，请重新登录。" }
{ "code": 500, "msg": "最小值1" }
```

---

## 四、推广 - 推广返佣记录

### 接口信息

| 字段 | 内容 |
|------|------|
| 方法 | `GET` |
| 路径 | `/api/promotinoLevel/myPromotionRecord` |
| 认证 | 必须登录 |

> ⚠️ 注意：路径中 `promotinoLevel` 是固定拼写（少一个字母），保持原样

### 请求参数（Query）

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| page | Integer | 否 | 1 | 页码 |
| size | Integer | 否 | 10 | 每页数量 |

### 成功响应（分页）

```json
{
  "rows": [
    {
      "id": 1,
      "subordinateUserId": 456,
      "rechargePrice": 100.00,
      "rebate": 5.00,
      "status": "已结算",
      "createTime": "2026-01-01 09:00:00"
    }
  ],
  "total": 30
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 记录ID |
| subordinateUserId | Integer | 下级用户ID |
| rechargePrice | BigDecimal | 下级本次充值金额 |
| rebate | BigDecimal | 你获得的返佣金额 |
| status | String | 记录状态 |
| createTime | String | 发生时间 |

### 错误响应示例

```json
{ "code": 401, "msg": "登录过期，请重新登录。" }
```

---

## 快速对照表

| 需求 | 方法 | 接口路径 | 是否分页 | 关键入参 |
|------|------|---------|---------|---------|
| 幸运升级-直接升级 | POST | `/api/luckyUpgrade/upgrade` | 否 | `upgradeOrnamentId`, `price`, `probability` |
| 对战-十佳记录 | GET | `/api/fight/myTopTen` | 否（固定10条） | 无 |
| 背包-分解记录 | POST | `/api/userPackSack/decomposeLog` | 是（rows+total） | `page`, `size` |
| 推广-返佣记录 | GET | `/api/promotinoLevel/myPromotionRecord` | 是（rows+total） | `page`, `size` |
| 高光时刻 | GET | `/api/user/gloryMoments` | 否 | 无 |
| 流水统计 | GET | `/api/user/flowStatistics` | 否 | 无 |
| 高光时刻TOP10 | GET | `/api/user/top10GloryMoments` | 否（固定10条） | 无 |

---

## 五、高光时刻

### 接口信息

| 字段 | 内容 |
|------|------|
| 方法 | `GET` |
| 路径 | `/api/user/gloryMoments` |
| 认证 | 必须登录 |

### 请求参数

无

### 成功响应

```json
{
  "code": 200,
  "data": [
    {
      "boxRecordId": 100001,
      "userId": 123,
      "ornamentName": "AK-47 | 火神 (崭新出厂)",
      "ornamentsPrice": 800.00,
      "imageUrl": "https://...",
      "ornamentsLevelId": 1,
      "levelImg": "https://...gold.png",
      "source": 1,
      "boxName": "精英箱",
      "createTime": "2026-04-08 20:00:00"
    }
  ]
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| boxRecordId | Long | 开箱记录ID |
| ornamentName | String | 饰品名称 |
| ornamentsPrice | BigDecimal | 饰品价值 |
| imageUrl | String | 饰品图片URL |
| ornamentsLevelId | Integer | 品质等级ID（固定为 1=金色） |
| levelImg | String | 品质图标URL |
| source | Integer | 来源：1=开箱，2=对战，其他见枚举 |
| boxName | String | 来源箱子名称 |
| createTime | String | 开出时间 |

> 返回近 **7日**内开出的所有金色饰品，按时间倒序，不分页

### 错误响应示例

```json
{ "code": 401, "msg": "登录过期，请重新登录。" }
```

---

## 六、流水统计

### 接口信息

| 字段 | 内容 |
|------|------|
| 方法 | `GET` |
| 路径 | `/api/user/flowStatistics` |
| 认证 | 必须登录 |

### 请求参数

无

### 成功响应

```json
{
  "code": 200,
  "data": {
    "todayFlow": 120.50,
    "yesterdayFlow": 360.00,
    "totalRecharge": 5000.00,
    "yesterdayReward": 12.50,
    "todayExpectedReward": 6.00
  }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| todayFlow | BigDecimal | 今日消费流水（当日 00:00 至今的支出合计，含彩虹转盘） |
| yesterdayFlow | BigDecimal | 昨日消费流水（昨日 00:00~24:00 支出合计） |
| totalRecharge | BigDecimal | 累计总充值金额 |
| yesterdayReward | BigDecimal | 昨日已发放的VIP消费返点金额 |
| todayExpectedReward | BigDecimal | 今日预期VIP返点（今日消费产生，次日凌晨发放到账） |

### 错误响应示例

```json
{ "code": 401, "msg": "登录过期，请重新登录。" }
```

---

## 九、高光时刻TOP10

### 接口信息

| 字段 | 内容 |
|------|------|
| 方法 | `GET` |
| 路径 | `/api/user/top10GloryMoments` |
| 认证 | 必须登录 |

### 请求参数

无

### 成功响应

```json
{
  "code": 200,
  "data": [
    {
      "boxRecordId": 100088,
      "ornamentName": "AWP | 龙焰 (崭新出厂)",
      "ornamentsPrice": 3500.00,
      "imageUrl": "https://...",
      "source": 1,
      "sourceName": "玩转盲盒",
      "createTime": "2026-04-20 18:30:00"
    },
    {
      "boxRecordId": 100055,
      "ornamentName": "AK-47 | 火神 (崭新出厂)",
      "ornamentsPrice": 800.00,
      "imageUrl": "https://...",
      "source": 2,
      "sourceName": "急速对战",
      "createTime": "2026-04-19 14:22:00"
    }
  ]
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| boxRecordId | Long | 开箱记录ID |
| ornamentName | String | 饰品名称 |
| ornamentsPrice | BigDecimal | 饰品价值 |
| imageUrl | String | 饰品图片URL |
| source | Integer | 来源编号 |
| sourceName | String | 来源名称（中文）：玩转盲盒 / 急速对战 / Roll房 / 幸运升级 / 系统发放 / 商城兑换 / 汰换 / 注册赠送福利 / 夺冠之路 / 贴纸 / 彩虹转盘 / 新人实名福利 |
| createTime | String | 获得时间 |

> 返回近 **7日**内价值最高的 **TOP10** 饰品，按 `ornamentsPrice` 降序排列，不分页，**不限饰品颜色**

### 错误响应示例

```json
{ "code": 401, "msg": "登录过期，请重新登录。" }
```

---

## 七、修改密码（含短信验证码）

> 修改密码需要先获取短信验证码，再携带验证码调用修改密码接口。

---

### 7.1 发送修改密码验证码

- **Method**：`POST`
- **Path**：`/api/sms/getVerifyCode`
- **Auth**：不需要 Token（`@Anonymous`）

#### 请求体

```json
{
  "phoneNumber": "13800000000",
  "type": "6"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| phoneNumber | String | 是 | 账号绑定的手机号 |
| type | String | 是 | 固定传 `"6"`（修改密码场景） |

#### 响应示例

```json
{ "code": 200, "msg": "获取验证码成功，请注意短信通知！" }
```

| 错误情况 | msg |
|----------|-----|
| 手机号为空 | 手机号不能为空 |
| 手机号格式错误 | 手机号格式错误，请检查手机号是否输入正确！ |
| 手机号未注册 | 该手机号未在本站注册！ |
| 60秒内重复发送 | 验证码已发送，请注意手机短信信息！ |

---

### 7.2 修改密码

- **Method**：`POST`
- **Path**：`/api/user/changePW`
- **Auth**：需要 Token

#### 请求体

```json
{
  "password": "新密码",
  "passwordAgain": "新密码确认",
  "code": "1234"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| password | String | 是 | 新密码（明文） |
| passwordAgain | String | 是 | 新密码确认，需与 `password` 一致 |
| code | String | 是 | 短信验证码（4位数字） |

#### 成功响应

```json
{ "code": 200, "msg": "修改密码成功。" }
```

> 修改成功后服务端会立即清除当前 Token，前端需跳转到登录页重新登录。

#### 失败响应

| 情况 | msg |
|------|-----|
| 验证码为空 | 验证码不能为空。 |
| 验证码格式错误 | 验证码格式错误。 |
| 验证码错误/过期 | 验证码错误 |
| 两次密码不一致 | 两次输入不一致。 |
| 账号未绑定手机号 | 该账号未绑定手机号，无法验证身份。 |
| 数据库更新失败 | 修改密码失败，请稍后重试。 |

---

## 八、充值明细列表（含排序）

- **Method**：`POST`
- **Path**：`/api/userAmountRecords/rechargeRecords`
- **Auth**：需要 Token

### 请求体

```json
{
  "page": 1,
  "size": 10,
  "status": 4,
  "orderBy": 1
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | Integer | 是 | 页码，最小 1 |
| size | Integer | 是 | 每页条数，最小 1 |
| status | Integer | 是 | 订单状态（4=支付成功） |
| orderBy | Integer | 否 | 排序方式，见下表，默认 1 |

**orderBy 取值说明：**

| 值 | 排序规则 |
|----|----------|
| 1（默认） | 时间降序（最新在前） |
| 2 | 时间升序（最早在前） |
| 3 | 金额降序（最大在前） |
| 4 | 金额升序（最小在前） |

### 成功响应

```json
{
  "code": 200,
  "data": [
    {
      "id": 1001,
      "userId": 888,
      "type": "1",
      "goodsId": 5,
      "goodsPrice": 100.00,
      "goodsNum": 1,
      "totalAmount": 100.00,
      "orderId": "ORDER20260401001",
      "outTradeNo": "THIRDPARTY202604001",
      "status": "4",
      "createTime": "2026-04-01 12:00:00",
      "updateTime": "2026-04-01 12:01:00"
    }
  ]
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer | 订单ID |
| userId | Integer | 用户ID |
| type | String | 支付类型（0卡密 1支付宝 2微信） |
| goodsPrice | BigDecimal | 商品单价 |
| goodsNum | Integer | 商品数量 |
| totalAmount | BigDecimal | 订单总金额 |
| orderId | String | 系统订单号 |
| outTradeNo | String | 第三方外部订单号 |
| status | String | 支付状态 |
| createTime | Date | 下单时间 |
| updateTime | Date | 支付完成时间 |

### 失败响应

```json
{ "code": 401, "msg": "登录过期，请重新登录。" }
```

---

## 快速对照表

| 序号 | 功能 | Method | Path | Auth |
|------|------|--------|------|------|
| 一 | 幸运升级（直接） | POST | `/api/luckyUpgrade/upgradeProcess` | 需要 |
| 二 | 幸运升级（背包） | POST | `/api/luckyUpgrade/upgradeProcess2` | 需要 |
| 三 | 获取十佳对战记录 | GET | `/api/user/myTopTenFights` | 需要 |
| 四 | 高光时刻 | GET | `/api/user/gloryMoments` | 需要 |
| 五 | 流水统计 | GET | `/api/user/flowStatistics` | 需要 |
| 六 | 发送修改密码验证码 | POST | `/api/sms/getVerifyCode` | 不需要 |
| 七 | 修改密码 | POST | `/api/user/changePW` | 需要 |
| 八 | 充值明细列表 | POST | `/api/userAmountRecords/rechargeRecords` | 需要 |
| 九 | 高光时刻TOP10 | GET | `/api/user/top10GloryMoments` | 需要 |
